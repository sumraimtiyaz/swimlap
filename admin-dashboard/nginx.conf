server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  # Single-page app: unknown paths fall back to index.html so client-side
  # routing (/swims/:id, /accounts) works on a hard refresh.
  location / {
    try_files $uri /index.html;
  }
}
